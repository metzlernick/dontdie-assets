# LEFT ARM Production Guardrail / Baseline Record

## ARCHIVED HISTORICAL RECORD
Current production authority is `../LEFT_ARM_ZERO_CONTACT_WORKFLOW.md` and the 4×1 prompt template. This file preserves the older 4×4 calibration lessons only.

Historical conclusions retained:
- LEFT ARM uses zero-contact generation with visible hero/context and no AI Stage B.
- Semantic real-world size must never inflate source art toward normal standalone prop scale.
- A local BRIEF FAIL does not justify redesigning the category architecture.
- If prompt correction causes scale/architecture regression, return to the last passing baseline.

The former Regression A family was golden shield / money bag / spatula / lantern. Current production has moved to one brief → four variants → 4×1 with a literal tiny-footprint lock.
